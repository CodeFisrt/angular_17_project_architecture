import { IsEditDirective } from './is-edit.directive';

describe('IsEditDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEditDirective();
    expect(directive).toBeTruthy();
  });
});
